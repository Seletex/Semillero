const idTdPantalla = "tdPantalla";

function controlarEvento(element){

    //alert(isDi
    let text = getText(element);
    if (isDigit(text)){
setNumber(text);
    }
    git(getText(element));
}

function setNumber(digit){
setText(idTdPantalla,getElementById(idTdPantalla)+ text);
}